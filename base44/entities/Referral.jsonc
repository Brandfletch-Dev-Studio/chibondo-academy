{
  "name": "Referral",
  "type": "object",
  "properties": {
    "referrer_id": {
      "type": "string",
      "description": "User ID of the person who referred"
    },
    "referrer_name": {
      "type": "string"
    },
    "referred_email": {
      "type": "string"
    },
    "referred_user_id": {
      "type": "string",
      "description": "User ID once the referred person registers"
    },
    "referred_name": {
      "type": "string"
    },
    "referral_code": {
      "type": "string",
      "description": "Unique code used by the referrer"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "registered",
        "paid",
        "rewarded"
      ],
      "default": "pending"
    },
    "reward_amount": {
      "type": "number",
      "default": 0
    },
    "reward_status": {
      "type": "string",
      "enum": [
        "pending",
        "paid",
        "cancelled"
      ],
      "default": "pending"
    },
    "notes": {
      "type": "string"
    },
    "recurring_reward_amount": {
      "type": "number",
      "default": 0,
      "description": "Total recurring commissions earned from this referral's renewals"
    },
    "recurring_count": {
      "type": "number",
      "default": 0,
      "description": "Number of renewal payments that have earned recurring commission"
    }
  },
  "required": [
    "referrer_id",
    "referral_code"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "data.referred_user_id": "{{user.id}}"
        }
      ]
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "teacher"
          }
        },
        {
          "user_condition": {
            "role": "user"
          }
        },
        {
          "data.referrer_id": "{{user.id}}"
        }
      ]
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}